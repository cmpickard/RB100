#the code won't print anything to the console b/c the method will stop executing at the "return" line
#the method will get popped from the stack at the return line and the puts statement won't
#be evaluated.