#The first program should print out '3' for x
#The second program will throw an error. That's b/c the variable a was first
#initialized inside the code block, and so once you're outside the code block,
#you can no longer call that variable, it's outside of scope.